/*
package com.example.birthdayApp

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button
import android.widget.TextView
import com.example.secondbirthday.R

class qoutesActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_qoutes_activity)
        val quotes: TextView = findViewById(R.id.editTextTextMultiLine)

        quotes.text = quoteAndAuthor
        editTextTextMultiLine
    }

    class MainActivity : AppCompatActivity() {
        var quoteAndAuthor: String = "No Quote"
        override fun onCreate(savedInstanceState: Bundle?) {
            super.onCreate(savedInstanceState)
            setContentView(R.layout.activity_main)
            val tickCard: Button = generateButtonReference(R.id.button)
            setClickListener(tickCard)
        }
}*/
